<?xml version="1.0" encoding="UTF-8"?>
<!--
 /**
  * Copyright © Magento, Inc. All rights reserved.
  * See COPYING.txt for license details.
  */
-->

<tests xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:mftf:Test/etc/testSchema.xsd">
    <test name="CreateSimpleProductTest">
        <annotations>
            <features value="Catalog"/>
            <stories value="Create Product"/>
            <title value="Admin should be able to create simple product."/>
            <description value="Admin should be able to create simple product."/>
            <severity value="MAJOR"/>
            <group value="Catalog"/>
            <group value="alex" />
        </annotations>
        <before>
            <!-- Login to Admin panel -->
            <actionGroup ref="LoginAsAdmin" stepKey="loginToAdminPanel" />
        </before>
        <after>
            <!-- Logout from Admin panel -->
        </after>

        <!-- Navigate to Catalog -> Products page (or just open by link) -->
        <amOnPage url="{{AdminProductIndexPage.url}}" stepKey="openProductGridPage" />

        <!-- Click "Add Product" button -->
        <click selector="{{AdminProductGridActionSection.addProductBtn}}" stepKey="clickAddProductButton" />
        <waitForPageLoad stepKey="waitForNewProductPageOpened" />

        <!-- Fill field "Name" with "Simple Product %unique_value%" -->
        <fillField selector="{{AdminProductFormSection.productName}}" userInput="Simple Product 12412431" stepKey="fillNameField" />

        <!-- Fill field "SKU" with "simple_product_%unique_value%" -->
        <fillField selector="{{AdminProductFormSection.productSku}}" userInput="simple-product-12412431" stepKey="fillSKUField" />

        <!-- Fill field "Price" with "500.00" -->
        <fillField selector="{{AdminProductFormSection.productPrice}}" userInput="500.00" stepKey="fillPriceField" />

        <!-- Fill field "Quantity" with "100" -->
        <fillField selector="{{AdminProductFormSection.productQuantity}}" userInput="100" stepKey="fillQtyField" />

        <!-- Fill field "Weight" with "100" -->
        <fillField selector="{{AdminProductFormSection.productWeight}}" userInput="100" stepKey="fillWeightField" />

        <!-- Click "Save" button -->
        <click selector="{{AdminProductFormSection.save}}" stepKey="clickSaveButton" />
        <waitForPageLoad stepKey="waitForProductIsSaved" />
        <!-- See success save message "You saved the product." -->
        <see selector="{{AdminMessagesSection.success}}" userInput="You saved the product." stepKey="seeSuccessMessage" />

        <!-- Navigate to Catalog -> Products page (or just open by link) -->
        <amOnPage url="{{AdminProductIndexPage.url}}" stepKey="openProductGridPageToVerifySavedProduct" />

        <!-- See created product is in grid -->
        <click selector="{{AdminProductGridFilterSection.filters}}" stepKey="clickFiltersButton" />

        <!-- See "Name" in grid is valid -->
        <fillField selector="{{AdminProductGridFilterSection.nameFilter}}" userInput="Simple Product 12412431" stepKey="fillNameFilterField" />

        <!-- See "SKU" in grid is valid -->
        <fillField selector="{{AdminProductGridFilterSection.skuFilter}}" userInput="simple-product-12412431" stepKey="fillSkuFilterField" />

        <!-- See "Price" in grid is valid -->
        <fillField selector="{{AdminProductGridFilterSection.priceFilterFrom}}" userInput="500.00" stepKey="fillPriceFromFilterField" />
        <fillField selector="{{AdminProductGridFilterSection.priceFilterTo}}" userInput="500.00" stepKey="fillPriceToFilterField" />

        <!-- See "Quantity" in grid is valid -->
        <fillField selector="{{AdminProductGridFilterSection.quantityFrom}}" userInput="100" stepKey="fillQtyFromFilterField" />
        <fillField selector="{{AdminProductGridFilterSection.quantityTo}}" userInput="100" stepKey="fillQtyToFilterField" />

        <click selector="{{AdminProductGridFilterSection.applyFilters}}" stepKey="clickApplyFilterButton" />
        <waitForPageLoad stepKey="waitForFiltersApplied" />

        <dontSee selector="{{AdminDataGridTableSection.dataGridEmpty}}" userInput="We couldn't find any records." stepKey="dontSeeCouldntFindMessage" />

        <!-- Open Storefront Product Page and verify "Name", "SKU", "Price" -->
        <amOnPage url="simple-product-12412431.html" stepKey="openProductOnStorefront" />
        <see selector="{{StorefrontProductInfoMainSection.productName}}" userInput="Simple Product 12412431" stepKey="seeProductName" />
        <see selector="{{StorefrontProductInfoMainSection.productSku}}" userInput="simple-product-12412431" stepKey="seeProductSku" />
        <see selector="{{StorefrontProductInfoMainSection.productPrice}}" userInput="$500.00" stepKey="seeProductPrice" />
    </test>
</tests>
